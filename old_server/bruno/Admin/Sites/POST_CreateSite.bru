meta {
  name: Create Site
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/sites
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwtToken}}
}

body:json {
  {
    "name": "My New Site"
  }
}
