meta {
  name: Update Tag
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/tags/tag-001
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwtToken}}
}

body:json {
  {
    "tagName": "updated-tag"
  }
}
